import 'package:flutter/material.dart';

class SlidesStyle extends StatelessWidget {
  const SlidesStyle({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
